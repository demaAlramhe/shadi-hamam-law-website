/**
 * Formspree — contact form endpoint (AJAX / JSON).
 *
 * 1. Create a form at https://formspree.io (free tier is fine).
 * 2. Copy your form URL (looks like https://formspree.io/f/xxxxxxxx).
 * 3. Paste it below as the string value.
 *
 * The form should accept fields: name, email, phone, message (names must match
 * the JSON keys sent from `Contact.jsx`).
 */
export const FORMSPREE_CONTACT_ENDPOINT = 'https://formspree.io/f/mjgpqado'
